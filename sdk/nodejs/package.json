{
    "name": "@jaxxstorm/pulumi-productionapp",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource productionapp ${VERSION}"
    },
    "dependencies": {
        "@pulumi/kubernetes": "^3.0.0"
    },
    "devDependencies": {
        "typescript": "^3.7.0"
    },
    "peerDependencies": {
        "@pulumi/pulumi": "latest"
    },
    "pulumi": {
        "resource": true
    }
}
